---
import { getCollection, getEntry, render } from 'astro:content';
import Layout from '../../../layouts/Layout.astro';

const intro = await getEntry('nutricion', 'macronutrientes/grasas-o-lipidos/que-son');
const { Content } = await render(intro!);

const subArticulos = (await getCollection('nutricion', ({ data, id }) =>
  !data.draft &&
  id.startsWith('macronutrientes/grasas-o-lipidos/') &&
  id !== 'macronutrientes/grasas-o-lipidos/que-son'
)).sort((a, b) => (a.data.title ?? '').localeCompare(b.data.title ?? '', 'es'));
---

<Layout
  title="Grasas o lípidos"
  description="Qué son las grasas, tipos, cuántas necesitas al día, qué son los ácidos grasos esenciales y qué fuentes alimentarias priorizar."
>
  <div class="container">
    <div class="cabecera-categoria">
      <nav class="migas">
        <a href="/">Inicio</a>
        <span class="sep">›</span>
        <a href="/nutricion">Nutrición</a>
        <span class="sep">›</span>
        <a href="/nutricion/macronutrientes">Macronutrientes</a>
        <span class="sep">›</span>
        <span>Grasas o lípidos</span>
      </nav>
      <h1>Grasas o lípidos</h1>
    </div>

    <div class="intro-prosa prosa seccion">
      <Content />
    </div>

    {subArticulos.length > 0 && (
      <section class="seccion seccion-tipos">
        <h2 class="titulo-seccion">Profundiza</h2>
        <div class="grid-articulos">
          {subArticulos.map(e => (
            <a href={`/nutricion/${e.id}`} class="tarjeta tarjeta-articulo">
              <div class="tarjeta-articulo-inner">
                <h2>{e.data.title}</h2>
                {e.data.description && <p>{e.data.description}</p>}
                <span class="leer-mas">Leer artículo →</span>
              </div>
            </a>
          ))}
        </div>
      </section>
    )}
  </div>
</Layout>

<style>
  .seccion-tipos {
    border-top: 1px solid var(--borde);
  }
</style>
